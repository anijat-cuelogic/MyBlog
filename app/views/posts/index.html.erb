<h1>Blog AnyThing..!!</h1>

<% @posts.each do |post| %>
  <h2><%= link_to post.title, post_path(post.to_param) %></h2>
  <%if post.image.present? %>
    <p>
      <%= image_tag post.image.url(:medium) %>
    </p>
  <% end %>
  <p>
    <%= post.description %>
  </p>
  <p><%= image_tag post.user.image.url(:thumb) if post.user.image.present? %>-<%= post.user.name%></p>
<% end %>
<br>
<%= link_to 'New Post', new_post_path %>